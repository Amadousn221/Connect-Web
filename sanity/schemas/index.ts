import type { SchemaTypeDefinition } from 'sanity';

// Les schémas de contenu (blogPost, resource, author, blogCategory, blogTag,
// resourceCategory, objects réutilisables, blocs Portable Text) sont créés à
// l'ÉTAPE 2. Ce tableau est volontairement vide pour l'instant — le Studio
// affichera "No schema types found", c'est attendu.
export const schemaTypes: SchemaTypeDefinition[] = [];
